This supposes that you have a working TCP server (echo server at least) in
Erlang: server.erl.

If you don't have one, make one first!

Then you can complete the stub (client.cpp) to connect to it and send data.

